/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Pour un hébergement statique (GitHub Pages, etc.), décommenter :
  // output: 'export',
  // images: { unoptimized: true },
};

export default nextConfig;
